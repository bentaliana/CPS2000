// 1. Good comments
/* A valid block comment */
// A valid line comment

// 2. Unterminated/malformed comments
/* Unterminated
/* Nested /* comment */ */    // if no nesting support, should error

// 3. Good literals
let i:int = 42;
let f:float = 3.14;
let c:colour = #abcdef;

// 4. Malformed literals
let badFloat:float = 123.;
// → ERROR_INVALID_FLOAT

let badColor:colour = #123abg;
// → ERROR_INVALID_COLOUR

// 5. Invalid characters
let x = 5 @ 3;
// → ERROR for '@'

// 6. Operators & punctuation
+-*/==!=<=>=->;:,().{}[]

// 7. Leading underscore identifiers
let _goodName:int = 0;
let __builtin:int = 1;

// 8. Whitespace & newline (ensure they’re skipped)
// (you won’t see tokens for spaces/tabs/newlines unless debug=true)
